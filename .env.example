# ─── Multi-Omics Pipeline Environment Variables ──────────────────────────────
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.

# ── Groq LLM (required for /biomarkers and /literature endpoints) ────────────
# Obtain from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama3-70b-8192
GROQ_AUDIT_MODEL=openai/gpt-oss-20b
GROQ_TEMPERATURE=0.0
GROQ_MAX_TOKENS=1024

# ── NCBI Entrez (required for PubMed retrieval) ──────────────────────────────
# Must be a real email to comply with NCBI Terms of Service
ENTREZ_EMAIL=madhavkannank2903@gmail.com
# Optional — increases rate limit from 3 req/s to 10 req/s
# Obtain from: https://www.ncbi.nlm.nih.gov/account/
ENTREZ_API_KEY=your_ncbi_api_key_here
PUBMED_MAX_ABSTRACTS=5

# ── TCGA / GDC ───────────────────────────────────────────────────────────────
TCGA_PROJECT_ID=TCGA-BRCA

# ── UCSC Xena (Alternative data source: 2200+ datasets) ──────────────────────
# Available hubs: tcgaHub | pancanHub | publicHub
XENA_HUB=tcgaHub
DATA_SOURCE=gdc  # or "xena" to use Xena instead of GDC

# ── MLflow tracking ──────────────────────────────────────────────────────────
MLFLOW_TRACKING_URI=http://localhost:5000

# ── FastAPI server ───────────────────────────────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8000

# ── Data preprocessing ───────────────────────────────────────────────────────
N_TOP_GENES=2000
MIN_VARIANCE_THRESHOLD=0.1

# ── Model training ───────────────────────────────────────────────────────────
CV_FOLDS=5
TOP_GENES_PER_FOLD=30
MIN_FOLD_APPEARANCES=3

# ── Reproducibility ──────────────────────────────────────────────────────────
RANDOM_SEED=42
